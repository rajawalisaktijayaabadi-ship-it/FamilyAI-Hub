version: '3.8'

services:
  family-ai-hub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: family-ai-hub
    ports:
      - '3000:3000'
    environment:
      - PORT=3000
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always
